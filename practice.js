function averageLength(arr) { 
    let average = Math.round(arr.reduce((a,b)=>a + b.length,0)/arr.length)
   
   return arr.map(e => e[0].repeat(average) )
   }function averageLength(arr) { 
    let average = Math.round(arr.reduce((a,b)=>a + b.length,0)/arr.length)
   
   return

   function averageLength(arr) { 
    let average = Math.round(arr.reduce((a,b)=>a + b.length,0)/arr.length)
   
   return arr.map(e => e[0].repeat(average) )
   }function averageLength(arr) { 
    let average = Math.round(arr.reduce((a,b)=>a + b.length,0)/arr.length)
   
   return
   function averageLength(arr) { 
    let average = Math.round(arr.reduce((a,b)=>a + b.length,0)/arr.length)
   
   return arr.map(e => e[0].repeat(average) )
   }function averageLength(arr) { 
    let average = Math.round(arr.reduce((a,b)=>a + b.length,0)/arr.length)
   
   return
   function averageLength(arr) { 
    let average = Math.round(arr.reduce((a,b)=>a + b.length,0)/arr.length)
   
   return arr.map(e => e[0].repeat(average) )
   }function averageLength(arr) { 
    let average = Math.round(arr.reduce((a,b)=>a + b.length,0)/arr.length)
   
   return

   function averageLength(arr) { 
    let average = Math.round(arr.reduce((a,b)=>a + b.length,0)/arr.length)
   
   return arr.map(e => e[0].repeat(average) )
   }function averageLength(arr) { 
    let average = Math.round(arr.reduce((a,b)=>a + b.length,0)/arr.length)
   
   return

   function averageLength(arr) { 
    let average = Math.round(arr.reduce((a,b)=>a + b.length,0)/arr.length)
   
   return arr.map(e => e[0].repeat(average) )
   }function averageLength(arr) { 
    let average = Math.round(arr.reduce((a,b)=>a + b.length,0)/arr.length)
   
   return
   function averageLength(arr) { 
    let average = Math.round(arr.reduce((a,b)=>a + b.length,0)/arr.length)
   
   return arr.map(e => e[0].repeat(average) )
   }function averageLength(arr) { 
    let average = Math.round(arr.reduce((a,b)=>a + b.length,0)/arr.length)
   
   return
   function averageLength(arr) { 
    let average = Math.round(arr.reduce((a,b)=>a + b.length,0)/arr.length)
   
   return arr.map(e => e[0].repeat(average) )
   }function averageLength(arr) { 
    let average = Math.round(arr.reduce((a,b)=>a + b.length,0)/arr.length)
   
   return

   function averageLength(arr) { 
    let average = Math.round(arr.reduce((a,b)=>a + b.length,0)/arr.length)
   
   return arr.map(e => e[0].repeat(average) )
   }function averageLength(arr) { 
    let average = Math.round(arr.reduce((a,b)=>a + b.length,0)/arr.length)
   
   return
   function averageLength(arr) { 
    let average = Math.round(arr.reduce((a,b)=>a + b.length,0)/arr.length)
   
   return arr.map(e => e[0].repeat(average) )
   }function averageLength(arr) { 
    let average = Math.round(arr.reduce((a,b)=>a + b.length,0)/arr.length)
   
   return
   function averageLength(arr) { 
    let average = Math.round(arr.reduce((a,b)=>a + b.length,0)/arr.length)
   
   return arr.map(e => e[0].repeat(average) )
   }function averageLength(arr) { 
    let average = Math.round(arr.reduce((a,b)=>a + b.length,0)/arr.length)
   
   return
   function averageLength(arr) { 
    let average = Math.round(arr.reduce((a,b)=>a + b.length,0)/arr.length)
   
   return arr.map(e => e[0].repeat(average) )
   }function averageLength(arr) { 
    let average = Math.round(arr.reduce((a,b)=>a + b.length,0)/arr.length)
   
   return

   function averageLength(arr) { 
    let average = Math.round(arr.reduce((a,b)=>a + b.length,0)/arr.length)
   
   return arr.map(e => e[0].repeat(average) )
   }function averageLength(arr) { 
    let average = Math.round(arr.reduce((a,b)=>a + b.length,0)/arr.length)
   
   return

   function averageLength(arr) { 
    let average = Math.round(arr.reduce((a,b)=>a + b.length,0)/arr.length)
   
   return arr.map(e => e[0].repeat(average) )
   }function averageLength(arr) { 
    let average = Math.round(arr.reduce((a,b)=>a + b.length,0)/arr.length)
   
   return
   function averageLength(arr) { 
    let average = Math.round(arr.reduce((a,b)=>a + b.length,0)/arr.length)
   
   return arr.map(e => e[0].repeat(average) )
   }function averageLength(arr) { 
    let average = Math.round(arr.reduce((a,b)=>a + b.length,0)/arr.length)
   
   return
   function averageLength(arr) { 
    let average = Math.round(arr.reduce((a,b)=>a + b.length,0)/arr.length)
   
   return arr.map(e => e[0].repeat(average) )
   }function averageLength(arr) { 
    let average = Math.round(arr.reduce((a,b)=>a + b.length,0)/arr.length)
   
   return
   function averageLength(arr) { 
    let average = Math.round(arr.reduce((a,b)=>a + b.length,0)/arr.length)
   
   return arr.map(e => e[0].repeat(average) )
   }function averageLength(arr) { 
    let average = Math.round(arr.reduce((a,b)=>a + b.length,0)/arr.length)
   
   return

   function averageLength(arr) { 
    let average = Math.round(arr.reduce((a,b)=>a + b.length,0)/arr.length)
   
   return arr.map(e => e[0].repeat(average) )
   }function averageLength(arr) { 
    let average = Math.round(arr.reduce((a,b)=>a + b.length,0)/arr.length)
   
   return
   function averageLength(arr) { 
    let average = Math.round(arr.reduce((a,b)=>a + b.length,0)/arr.length)
   
   return arr.map(e => e[0].repeat(average) )
   }function averageLength(arr) { 
    let average = Math.round(arr.reduce((a,b)=>a + b.length,0)/arr.length)
   
   return
   function averageLength(arr) { 
    let average = Math.round(arr.reduce((a,b)=>a + b.length,0)/arr.length)
   
   return arr.map(e => e[0].repeat(average) )
   }function averageLength(arr) { 
    let average = Math.round(arr.reduce((a,b)=>a + b.length,0)/arr.length)
   
   return
   function averageLength(arr) { 
    let average = Math.round(arr.reduce((a,b)=>a + b.length,0)/arr.length)
   
   return arr.map(e => e[0].repeat(average) )
   }function averageLength(arr) { 
    let average = Math.round(arr.reduce((a,b)=>a + b.length,0)/arr.length)
   
   return
   function averageLength(arr) { 
    let average = Math.round(arr.reduce((a,b)=>a + b.length,0)/arr.length)
   
   return arr.map(e => e[0].repeat(average) )
   }function averageLength(arr) { 
    let average = Math.round(arr.reduce((a,b)=>a + b.length,0)/arr.length)
   
   return
   function averageLength(arr) { 
    let average = Math.round(arr.reduce((a,b)=>a + b.length,0)/arr.length)
   
   return arr.map(e => e[0].repeat(average) )
   }function averageLength(arr) { 
    let average = Math.round(arr.reduce((a,b)=>a + b.length,0)/arr.length)
   
   return
   function averageLength(arr) { 
    let average = Math.round(arr.reduce((a,b)=>a + b.length,0)/arr.length)
   
   return arr.map(e => e[0].repeat(average) )
   }function averageLength(arr) { 
    let average = Math.round(arr.reduce((a,b)=>a + b.length,0)/arr.length)
   
   return
   function averageLength(arr) { 
    let average = Math.round(arr.reduce((a,b)=>a + b.length,0)/arr.length)
   
   return arr.map(e => e[0].repeat(average) )
   }function averageLength(arr) { 
    let average = Math.round(arr.reduce((a,b)=>a + b.length,0)/arr.length)
   
   return
   function averageLength(arr) { 
    let average = Math.round(arr.reduce((a,b)=>a + b.length,0)/arr.length)
   
   return arr.map(e => e[0].repeat(average) )
   }function averageLength(arr) { 
    let average = Math.round(arr.reduce((a,b)=>a + b.length,0)/arr.length)
   
   return
   function averageLength(arr) { 
    let average = Math.round(arr.reduce((a,b)=>a + b.length,0)/arr.length)
   
   return arr.map(e => e[0].repeat(average) )
   }function averageLength(arr) { 
    let average = Math.round(arr.reduce((a,b)=>a + b.length,0)/arr.length)
   
   return
   function averageLength(arr) { 
    let average = Math.round(arr.reduce((a,b)=>a + b.length,0)/arr.length)
   
   return arr.map(e => e[0].repeat(average) )
   }function averageLength(arr) { 
    let average = Math.round(arr.reduce((a,b)=>a + b.length,0)/arr.length)
   
   return

   function averageLength(arr) { 
    let average = Math.round(arr.reduce((a,b)=>a + b.length,0)/arr.length)
   
   return arr.map(e => e[0].repeat(average) )
   }function averageLength(arr) { 
    let average = Math.round(arr.reduce((a,b)=>a + b.length,0)/arr.length)
   
   return
   function averageLength(arr) { 
    let average = Math.round(arr.reduce((a,b)=>a + b.length,0)/arr.length)
   
   return arr.map(e => e[0].repeat(average) )
   }function averageLength(arr) { 
    let average = Math.round(arr.reduce((a,b)=>a + b.length,0)/arr.length)
   
   return


   function averageLength(arr) { 
    let average = Math.round(arr.reduce((a,b)=>a + b.length,0)/arr.length)
   
   return arr.map(e => e[0].repeat(average) )
   }function averageLength(arr) { 
    let average = Math.round(arr.reduce((a,b)=>a + b.length,0)/arr.length)
   
   return
   function averageLength(arr) { 
    let average = Math.round(arr.reduce((a,b)=>a + b.length,0)/arr.length)
   
   return arr.map(e => e[0].repeat(average) )
   }function averageLength(arr) { 
    let average = Math.round(arr.reduce((a,b)=>a + b.length,0)/arr.length)
   
   return
   function averageLength(arr) { 
    let average = Math.round(arr.reduce((a,b)=>a + b.length,0)/arr.length)
   
   return arr.map(e => e[0].repeat(average) )
   }function averageLength(arr) { 
    let average = Math.round(arr.reduce((a,b)=>a + b.length,0)/arr.length)
   
   return
   function averageLength(arr) { 
    let average = Math.round(arr.reduce((a,b)=>a + b.length,0)/arr.length)
   
   return arr.map(e => e[0].repeat(average) )
   }function averageLength(arr) { 
    let average = Math.round(arr.reduce((a,b)=>a + b.length,0)/arr.length)
   
   return
   function averageLength(arr) { 
    let average = Math.round(arr.reduce((a,b)=>a + b.length,0)/arr.length)
   
   return arr.map(e => e[0].repeat(average) )
   }function averageLength(arr) { 
    let average = Math.round(arr.reduce((a,b)=>a + b.length,0)/arr.length)
   
   return

   function averageLength(arr) { 
    let average = Math.round(arr.reduce((a,b)=>a + b.length,0)/arr.length)
   
   return arr.map(e => e[0].repeat(average) )
   }function averageLength(arr) { 
    let average = Math.round(arr.reduce((a,b)=>a + b.length,0)/arr.length)
   
   return

   function averageLength(arr) { 
    let average = Math.round(arr.reduce((a,b)=>a + b.length,0)/arr.length)
   
   return arr.map(e => e[0].repeat(average) )
   }function averageLength(arr) { 
    let average = Math.round(arr.reduce((a,b)=>a + b.length,0)/arr.length)
    function averageLength(arr) { 
        let average = Math.round(arr.reduce((a,b)=>a + b.length,0)/arr.length)
       
       return arr.map(e => e[0].repeat(average) )
       }function averageLength(arr) { 
        let average = Math.round(arr.reduce((a,b)=>a + b.length,0)/arr.length)
       
       return
    
   returnfunction averageLength(arr) { 
    let average = Math.round(arr.reduce((a,b)=>a + b.length,0)/arr.length)
   
   return arr.map(e => e[0].repeat(average) )
   }function averageLength(arr) { 
    let average = Math.round(arr.reduce((a,b)=>a + b.length,0)/arr.length)
   
   return

   function averageLength(arr) { 
    let average = Math.round(arr.reduce((a,b)=>a + b.length,0)/arr.length)
   
   return arr.map(e => e[0].repeat(average) )
   }function averageLength(arr) { 
    let average = Math.round(arr.reduce((a,b)=>a + b.length,0)/arr.length)
   
   return
   function averageLength(arr) { 
    let average = Math.round(arr.reduce((a,b)=>a + b.length,0)/arr.length)
   
   return arr.map(e => e[0].repeat(average) )
   }function averageLength(arr) { 
    let average = Math.round(arr.reduce((a,b)=>a + b.length,0)/arr.length)
   
   return





